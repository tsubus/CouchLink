story_id: e01s02
title: Select the advertised interface for this host session
spec: e01s02-session-interface-selector.md
status: failing
bcps: 3
risk: P1
tasks:
  - id: 1
    description: "Add RED runtime cases first, then add a contained non-null HostNetworkSelectionSnapshot and explicit runtime selection command covering NotStarted/automatic/manual/unavailable states, invalid IDs, atomic snapshot/advertisement consistency, explicit reselection, StopAsync clearing, and fresh-session reset."
    verify: "dotnet run --project src/windows/CouchLink.Host.Core.Tests/CouchLink.Host.Core.Tests.csproj --configuration Release -- --group manual-selection"
    risk: P1
    status: failing
    allure:
      severity: high
      categories:
        - "unit"
        - "Runtime State"
  - id: 2
    description: "Add RED presentation assertions first, then expose eligible candidates, selected item, selection mode, endpoint gating, actionable unavailable status, and the explicit selection command through MainWindowViewModel without reading or writing HostPreferences."
    verify: "dotnet run --project src/windows/CouchLink.Host.Core.Tests/CouchLink.Host.Core.Tests.csproj --configuration Release -- --group snapshot-presentation"
    risk: P1
    status: failing
    allure:
      severity: high
      categories:
        - "presentation"
        - "WPF"
  - id: 3
    description: "Bind the eligible-only selector and unavailable/reselection state in MainWindow.xaml, preserve existing dashboard/tray/pairing/trusted-device controls, and compile the snapshot producer and sole active consumer together."
    verify: "dotnet build src/windows/CouchLink.Host.Wpf/CouchLink.Host.Wpf.csproj --configuration Release"
    risk: P2
    status: failing
    allure:
      severity: normal
      categories:
        - "UI"
        - "WPF"
  - id: 4
    description: "Prove session-only lifetime and advertised-details-only behavior: no HostPreferences persistence, no fallback after loss, explicit reselection resumes discovery, and protocol/ports/broadcast/TCP binding/host-pairing-Wake identity remain unchanged."
    verify: "dotnet run --project src/windows/CouchLink.Host.Core.Tests/CouchLink.Host.Core.Tests.csproj --configuration Release -- --group session-boundaries && dotnet test src/windows/CouchLink.sln --configuration Release"
    risk: P1
    status: failing
    allure:
      severity: high
      categories:
        - "integration"
        - "Compatibility"
